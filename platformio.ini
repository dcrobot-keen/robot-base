; Placeholder — target board not yet chosen (see research.md, "아직 정하지 않은 것").
; Once hardware is selected, replace [env:esp32dev] below and implement src/main.cpp
; against the protocol prototyped in sim/src (frame.js + commands.js are the
; reference implementation, validated by the Node.js simulator before porting).

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
